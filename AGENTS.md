# Project Notes

## Versioning

- **Always increment the plugin version** whenever code changes are made (bug fixes, features, or settings changes).
- Update all of the following together:
  - Main plugin file header `Version:` and the `RL_VERSION` (or equivalent) constant.
  - `readme.txt` `Stable tag:`.
  - A new changelog entry in `readme.txt` describing the changes.
